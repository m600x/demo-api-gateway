apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-api-gateway.fullname" . }}
  labels:
    {{- include "demo-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-api-gateway.selectorLabels" . | nindent 4 }}
